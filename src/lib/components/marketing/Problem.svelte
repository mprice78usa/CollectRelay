<section class="problem">
	<div class="container-narrow">
		<h2>Document collection shouldn't feel like herding cats.</h2>
		<p>
			CollectRelay replaces endless email threads, "did you get it?" texts, and wrong-file uploads
			with one clean link, one checklist, and a clear status view for everyone involved.
		</p>
	</div>
</section>

<style>
	.problem {
		padding: var(--section-gap) 0;
		text-align: center;
	}

	h2 {
		font-size: clamp(var(--font-size-xxl), 3.5vw, var(--font-size-3xl));
		font-weight: 700;
		line-height: 1.2;
		letter-spacing: -0.02em;
		margin-bottom: var(--space-xl);
		color: var(--text-primary);
	}

	p {
		font-size: var(--font-size-lg);
		color: var(--text-secondary);
		line-height: 1.7;
		max-width: 600px;
		margin: 0 auto;
	}
</style>
