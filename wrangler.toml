name = "collectrelay"
pages_build_output_dir = ".svelte-kit/cloudflare"
compatibility_date = "2026-03-01"
compatibility_flags = ["nodejs_compat"]

# D1 Database
[[d1_databases]]
binding = "DB"
database_name = "collectrelay-db"
database_id = "426fdaa3-c751-40b8-939f-7097b98279ed"

# R2 Bucket
[[r2_buckets]]
binding = "FILES_BUCKET"
bucket_name = "collectrelay-files"

# KV Namespaces
[[kv_namespaces]]
binding = "SESSIONS"
id = "1bdad54acf4d40ce9069e5b36a301aaa"

[[kv_namespaces]]
binding = "MAGIC_LINKS"
id = "e319570c7c1643f991819053d6a607d4"

# Short-lived OAuth state tokens for cloud storage integrations (Box, Dropbox, Drive).
# 15-min TTL. For production, create with: npx wrangler kv namespace create OAUTH_STATES
# then replace the placeholder id below.
[[kv_namespaces]]
binding = "OAUTH_STATES"
id = "93124ce56ce04e469fb58c92641d0439"

# Workers AI
[ai]
binding = "AI"

# Note: Cron triggers are not supported in Pages projects.
# Use an external scheduler (e.g., cron-job.org) to call:
#   GET /api/cron/reminders (daily reminders)
#   GET /api/cron/rates (weekday mortgage rate fetch)
